import {Wrapper} from "../../components/Styles/Styles.component";
import styled from "styled-components";

export const CvWrapper = styled(Wrapper)`
display: grid;
grid-template-columns: 25rem 1fr;
margin-top: 2.6rem;
column-gap: .4rem;`;