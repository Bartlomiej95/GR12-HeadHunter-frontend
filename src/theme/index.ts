import {color} from "./color";
import {size} from "./size";
import {font} from "./font";
import {transition} from "./transition";

export const theme = {
    color,
    size,
    font,
    transition,
}